node_modules/
dist/
.astro/
.DS_Store
*.log

# Publish worktree, and the dry-run artifacts the journal CLI writes.
.publish/
journal/*.prompt.txt
journal/*.brief.json
journal/*.rejected.txt

# Beads / Dolt files (added by bd init). The whole tracker is external state,
# as in the rib: issues live in Dolt, nothing bead-related is committed. The
# agent skill under .agents/ is documentation and does travel with the repo.
.dolt/
*.db
.beads-credential-key
.beads/
